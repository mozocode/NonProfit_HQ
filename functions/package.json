{
  "name": "nonprofithq-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf lib",
    "lint": "eslint src --ext .ts",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "bootstrap-admin": "node scripts/bootstrapFirstAdmin.cjs",
    "pilot:entitlements": "node scripts/pilotEntitlements.cjs",
    "pilot:backfill-audit": "node scripts/backfillAuditEvents.cjs"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "eslint": "^10.0.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
