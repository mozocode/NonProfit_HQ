# Phase 3 seed data

Place domain-specific seed JSON or import from `runPhase3Seed.ts`.

- Use deterministic document IDs so seeds are idempotent.
- Every tenant doc must include `organizationId`.
- See `docs/PHASE3-SEED-STRATEGY.md` for write order and conventions.
