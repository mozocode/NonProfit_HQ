/**
 * Server-only modules: server actions, API route helpers, server-side Firebase admin.
 * Do not import this from client components.
 */

// Placeholder for server-side auth/session or Firebase Admin usage.
export const serverEnv = {
  nodeEnv: process.env.NODE_ENV,
};
